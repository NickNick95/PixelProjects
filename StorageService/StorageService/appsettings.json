{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "RabbitMQ": {
    "Host": "localhost", // set rabbitmq for using in docker
    "Password": "rmpassword",
    "UserName": "rmuser"
  },
  "LogFileName": "visits.log"
}
